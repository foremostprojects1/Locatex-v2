import SidebarListing from "./SidebarListing";

/** SidebarList — the shared listing page with the list tab open. */
export default function SidebarList() {
  return <SidebarListing defaultLayout="list" />;
}
