import TopmapListing from "./TopmapListing";

/** TopmapGrid — the shared listing page with the grid tab open. */
export default function TopmapGrid() {
  return <TopmapListing defaultLayout="grid" />;
}
