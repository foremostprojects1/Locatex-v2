import SidebarListing from "./SidebarListing";

/** SidebarGrid — the shared listing page with the grid tab open. */
export default function SidebarGrid() {
  return <SidebarListing defaultLayout="grid" />;
}
