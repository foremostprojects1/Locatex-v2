import TopmapListing from "./TopmapListing";

/** TopmapList — the shared listing page with the list tab open. */
export default function TopmapList() {
  return <TopmapListing defaultLayout="list" />;
}
